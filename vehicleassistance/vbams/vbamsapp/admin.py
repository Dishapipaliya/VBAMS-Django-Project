# admin.py
from django.contrib import admin
# from .models import VehicleRequest

# @admin.register(VehicleRequest)  # Register only once
# class VehicleRequestAdmin(admin.ModelAdmin):
#     list_display = ('vehicle_register_number', 'vehicle_model', 'vehicle_manufacturer', 'created_at')
#     ordering = ['created_at']
#     list_filter = ['created_at']

